import { Contact } from "@/components/site/Contact";

export const metadata = {
    title: "Contact Us | DineDiscover",
    description: "Get in touch with the DineDiscover team.",
};

export default function ContactPage() {
    return <Contact />;
}
